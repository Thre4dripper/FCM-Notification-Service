{
  "name": "fcm-notification-lambda",
  "version": "1.0.0",
  "description": "AWS Lambda function for FCM push notifications via API Gateway",
  "main": "dist/main.js",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "test": "pnpm run build && node test.js",
    "package": "pnpm run build && pnpm install --prod --node-linker=hoisted && zip -r lambda.zip dist/ node_modules/ && pnpm install",
    "deploy": "pnpm run package && aws lambda update-function-code --function-name $LAMBDA_FUNCTION_NAME --zip-file fileb://lambda.zip --region $AWS_REGION"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.161",
    "@types/node": "^22.19.19",
    "prettier": "^3.8.3",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "firebase-admin": "^12.7.0"
  }
}
